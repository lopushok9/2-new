<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>User Profile</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      background: #fff;
      color: #151515;
      font-family: 'Inter', Arial, sans-serif;
      display: flex;
      flex-direction: column;
      height: 100vh;
      width: 100vw;
      box-sizing: border-box;
    }
    .profile-wrap {
      flex: 1;
      display: flex;
      min-height: 100vh;
    }
    .profile-left {
      flex: 1 1 50%;
      background: linear-gradient(135deg, #f8fafc 20%, #ecfdf5 100%);
      display: none;
      align-items: center;
      justify-content: center;
    }
    .icon-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 3rem;
    }
    .circle-icon {
      width: 96px;
      height: 96px;
      background: #fff;
      border-radius: 24px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.3rem;
      box-shadow: 0 2px 8px 0 #e2e8f0;
      border: 1px solid #f1f5f9;
    }
    .profile-main {
      flex: 1 1 50%;
      min-width: 320px;
      display: flex;
      flex-direction: column;
    }
    .profile-content {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    .profile-card {
      width: 100%;
      max-width: 380px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 3rem;
    }
    .profile-header {
      text-align: center;
      margin-bottom: 1rem;
    }
    .profile-header-logo {
      width: 48px;
      height: 48px;
      border-radius: 12px;
      background: #0f172a;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.25rem;
    }
    .profile-header-logo span {
      font-size: 1.25rem;
    }
    .profile-header h1 {
      margin: 0 0 .5rem;
      font-size: 2rem;
      font-weight: 700;
      color: #111827;
    }
    .profile-header p {
      color: #64748b;
      margin: 0;
    }
    .avatar-row {
      display: flex;
      justify-content: center;
      margin-bottom: 1.5rem;
    }
    .profile-avatar {
      width: 80px;
      height: 80px;
      background: #f1f5f9;
      border-radius: 999px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2rem;
      border: 1.5px solid #e2e8f0;
      font-weight: 600;
      color: #334155;
    }
    .info-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      margin-bottom: 2rem;
    }
    .info-row {
      display: flex;
      align-items: center;
      gap: .75rem;
      padding: 1rem;
      background: #f8fafc;
      border-radius: 16px;
      border: 1px solid #f1f5f9;
    }
    .info-row svg {
      flex-shrink: 0;
      width: 20px;
      height: 20px;
      color: #94a3b8;
    }
    .info-text-label {
      font-size: 0.93rem;
      color: #64748b;
      margin: 0 0 2px;
    }
    .info-text-value {
      font-size: 1.08rem;
      color: #1e293b;
      font-weight: 500;
      margin: 0;
    }
    .profile-btn-edit {
      width: 100%;
      padding: 1rem;
      font-size: 1rem;
      font-weight: 500;
      border-radius: 14px;
      background: #0f172a;
      color: #fff;
      border: none;
      cursor: pointer;
      box-shadow: 0 2px 8px rgba(16, 19, 40, 0.06);
      display: flex;
      align-items: center;
      justify-content: center;
      gap: .5rem;
      transition: background .18s, box-shadow .18s, transform .18s;
    }
    .profile-btn-edit:hover {
      background: #1e293b;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(16, 19, 40, .17);
    }
    .footer {
      text-align: center;
      padding: 1.5rem 2rem 1rem;
      font-size: 0.85rem;
      color: #cbd5e1;
      border-top: 1px solid #f1f5f9;
      background: #fff;
    }
    @media (min-width: 1024px) {
      .profile-left {
        display: flex;
      }
    }
  </style>
</head>
<body>
  <div class="profile-wrap">
    <div class="profile-left">
      <div class="icon-grid">
        <div class="circle-icon">🐦</div>
        <div class="circle-icon">🦅</div>
        <div class="circle-icon">🦉</div>
        <div class="circle-icon">🦜</div>
      </div>
    </div>
    <main class="profile-main">
      <section class="profile-content">
        <div class="profile-card">
          <div class="profile-header">
            <div class="profile-header-logo">
              <span>🐦</span>
            </div>
            <h1>Your Profile</h1>
            <p>Manage your BirdScope account information</p>
          </div>
          <div>
            <div class="avatar-row">
              <div class="profile-avatar" id="user-avatar"><%= user.name.charAt(0) + (user.name.split(' ')[1] || '').charAt(0) %></div>
            </div>
            <div class="info-list">
              <div class="info-row">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M5.121 17.804A9 9 0 1 1 21 12a9 9 0 0 1-15.879 5.804ZM9 10a3 3 0 1 1 6 0 3 3 0 0 1-6 0Z"/>
                </svg>
                <div>
                  <div class="info-text-label">Full Name</div>
                  <div class="info-text-value" id="user-name"><%= user.name %></div>
                </div>
              </div>
              <div class="info-row">
                <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                  <rect width="20" height="14" x="2" y="5" rx="2"/>
                  <path d="m22 5-10 7L2 5"/>
                </svg>
                <div>
                  <div class="info-text-label">Email Address</div>
                  <div class="info-text-value" id="user-email"><%= user.email %></div>
                </div>
              </div>
            </div>
            <button class="profile-btn-edit" id="edit-profile-btn">
              <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" width="18" height="18">
                <path d="M16.475 5.408a3.381 3.381 0 0 1 4.785 4.784l-9.917 9.917-5.144.571.572-5.144ZM13.384 7.5l3.082 3.084"/>
              </svg>
              Edit Profile
            </button>
          </div>
        </div>
      </section>
      <footer class="footer">© 2024 BirdScope. Protecting avian biodiversity worldwide.</footer>
    </main>
  </div>
</body>
</html>
